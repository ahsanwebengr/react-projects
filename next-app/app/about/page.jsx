export const metadata = {
    title: 'About Us Page',
    description: 'This the about page of next js app.'
};
const page = () => {
    return (
        <div>About page</div>
    );
};

export default page;